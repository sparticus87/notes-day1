pretest notes
\
touch -t "$filedate"1730 $newfile

find $dirpath -type f -mtime -1 | sort <------ finds anything in dirpath directory that is a file, with a modified time within the last day, sorted



x3 grep
x3  find ---------------------------------------- find      -exec tar -czf $var {}  +                   -maxdepth 
pkill
tar -czf <--- zips up tarball
touch -d <-----------date
touch -t <-----------time
rm -r




















